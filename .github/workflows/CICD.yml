name: Blue Print of CICD

on:
 workflow_dispatch:

jobs:
 code:
  runs-on: ubuntu-latest
  steps:
   - name: Clone the code
     run: echo "Pull the code from github"
 build:
  runs-on: ubuntu-22.04
  steps:
    - name: Build the code
      run: echo "Build the application using docker"
 test:
  runs-on: ubuntu-22.04
  steps:
    - name: test the code
      run: echo "test the code"
 deploy:
  runs-on: ubuntu-22.04
  steps:
    - name: Deploy the code
      run: echo "Deploy the code into prod"
